主题：RAG 里的「召回」是啥（对照我们 IPO 助手的设计）

适用场景：招股书已经切块进向量库，你问问题，系统先找片段再回答。

一、先给三个硬事实（记这个就够应付一半）

招股书不会整本塞给模型：太长、太贵、还容易跑偏。

系统会把招股书切成很多小段，每段带页码之类的标签，先「搜」再「答」。

「召回」说的就是：**搜出来、捞回来的那几段材料**；没这一步，后面生成再强也像闭卷考试瞎蒙。

重要度：★★★★（类比 example 里的星级；指对答案质量影响大）

二、召回和生成，别混

召回：从库里把「和问题长得像」的段落捞出来，像你在 PDF 里智能版 Ctrl+F。

生成：模型根据捞出来的段落组织成人话。材料给错了，人话再漂亮也可能是错的。

所以行业里常说：召回是天花板之一——招股书几百页，**找对页**比「文笔好」更优先。

重要度：★★★★

三、那「Recall@k」又是啥？跟上面啥关系？

上面说的是**系统里真实发生的一步**（检索/召回）。

Recall@k 是**考试分数**：我们提前标好「正确答案在第 142 页那段」，然后看系统每次取前 k 段时，**有没有把那段捞进来**。

k 常取 5 或 10：意思是「我只允许模型看前 5 段材料」，看正确答案在不在里面。

命中 → 这次召回算过关；老不命中 → 该调分块大小、embedding、或者检索策略（文档 DESIGN.md 里那套）。

重要度：★★★（做 benchmark 才需要抠这个；平时理解流水线可以略过）

四、套到我们项目上：代码大概落在哪

向量相似度那一坨：`index/retriever.py`、向量库。

多轮对话：先把「它」「上面那个」改写成完整问题，再走同一套召回，再进 LangChain 的对话链。

想量一量好不好：`benchmarks/` 里用黄金集算 Recall@k。

重要度：★★★★（知道地图就行，不用背类名）

总结：

召回 = **先检索、捞片段**，不是模型写作文本身。

Recall@k = **用标准题测检索准不准**，属于评测，不是用户界面上的按钮。

和我们 `example.md` 那种打新文一样：**先翻对招股书（召回），再下判断（生成）**；翻错页，后面 stars 打得再热闹也可能是幻觉。
