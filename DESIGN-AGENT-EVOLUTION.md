# IPO 助手：从 RAG 演进到 Agent — 新增设计

本文档描述在现有 **RAG + 多轮对话**（见 `DESIGN.md`）之上的**演进方向**，用于后续迭代时对照，**非当前已实现功能**。

---

## 1. 现状与目标

| 维度 | 当前（v1） | 目标（Agent 向） |
|------|------------|------------------|
| 编排 | 固定链：检索 → 生成 | **多步循环**：观察 → 行动 → 再观察 |
| 决策 | 流程由代码写死 | **模型选择下一步**（调用何种工具、是否再检索） |
| 能力边界 | 招股书向量检索 + LLM | 检索 + **工具**（计算、外搜、拉取文件等） |
| 产品表述 | RAG 应用 / 智能问答 | 可对外称「IPO Agent」（技术表述建议：**Tool-calling Agent** 或 **LangGraph 工作流**） |

**不构成投资建议**、引用与免责声明要求与 `DESIGN.md` 一致。

---

## 2. 「算 Agent」的判定（内部对齐用）

满足越多，越接近常见工程意义上的 Agent：

1. **目标导向**：完成「分析 / 报告 / 多步任务」，而非单轮问答。
2. **多步循环**：根据中间结果决定下一步（而非单次 chain）。
3. **工具 / 环境**：可调用的外部能力（不限于向量检索）。
4. **由模型分配动作**：下一步是再检索、改 query、调 API 还是结束，**主要由 LLM 在约束下选择**（配合最大步数、预算）。

仅「调 Chat API」或仅「固定 RAG 一次检索」**不算**严格意义上的 Agent。

---

## 3. 演进支柱与可落地内容

### 3.1 工具（Tools）— 优先投入

为模型暴露 **可调用函数**（LangChain `@tool` / OpenAI function calling），例如：

| 类别 | 示例能力 | 说明 |
|------|----------|------|
| 招股书 | `search_prospectus(query)`、`read_page_range(start, end)` | 封装现有 Retriever / 元数据过滤 |
| 计算 | `calculator(expr)` 或受限表达式求值 | 估值、比例、简易中签估算等，避免胡算 |
| 外部信息 | `web_search(query)`（可选） | 行业新闻、可比公司；**必须标注来源与不确定性** |
| 自动化 | `fetch_prospectus_pdf(url_or_ticker)`（可选） | 与「主动拉招股书」管线对接 |

**原则**：先 **2～3 个工具** + 一条 Agent 回路，再扩展。

### 3.2 循环与编排（LangGraph 等）

用 **显式状态机** 替代「一条链跑到底」：

- 检索为空 → **改写 query** → 再检索（可先做规则/固定图，再交给模型选边）。
- 需要数字 → 调 **计算器** 工具。
- 需要时效信息 → 调 **搜索** 工具（若启用）。

推荐技术：**LangGraph**（分支、重试、人类介入节点）与现有 LangChain 模型、Retriever 集成。

### 3.3 规划层（第二阶段）

- 用户请求「全面分析」→ 先产出 **子任务列表**（业务 / 财务 / 风险 / 打新条款），再按子任务调用工具与检索。
- 实现可选：**Plan-and-execute**、两阶段 prompt（规划 → 执行），或轻量多 Agent（规划员 + 执行员），**在工具循环稳定后再做**。

### 3.4 记忆

- **会话内**：在现有 Message History 上增加 **滚动摘要**，控制 token。
- **跨会话（可选）**：用户确认过的事实、偏好写入向量库或轻量 KV，供后续会话检索。

### 3.5 自检与停止条件

- 关键结论与检索片段 **对齐检查**（规则或 LLM-as-judge）；对不齐则 **再检索** 或标「未披露」。
- **最大步数 / 超时 / 费用上限**，防止死循环。

---

## 4. 建议实施顺序（里程碑）

1. **M1**：工具化检索 + 计算器 + **Tool-calling Agent**（最小闭环）。
2. **M2**：LangGraph：**检索失败 → 改写 → 重试**；报告模式改为 **分节检索/分节生成**（可半自动）。
3. **M3**：可选外部搜索 / 招股书拉取工具；合规与频率限制。
4. **M4**：规划层、长期记忆、多角色（按需）。

---

## 5. 与现有仓库的对应关系

| 现有模块 | 演进时复用 |
|----------|------------|
| `index/retriever.py`、`vector_store` | 作为 `search_prospectus` 的后端 |
| `chains/chat_chain.py`、`report_chain.py` | 可保留为「无 Agent」降级路径；Agent 作为新入口（如 `ipo-rag agent`） |
| `ingestion/pipeline.py` | 与「拉取 PDF」工具或定时任务共用 |

---

## 6. 风险与合规

- 外搜与第三方 API 可能引入**非招股书**信息，须在输出中区分「招股书摘录」与「外部来源」。
- 工具调用增加 **失败面与延迟**，需超时与降级（回退到纯 RAG）。

---

## 7. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-04-12 | 首版：从 RAG 到 Agent 的支柱、工具表、里程碑与 `DESIGN.md` 的关系 |
